name: mcp
mode: catalog-only-not-active-bundle
profile: public-agent
skill_count: 5
skills:
- name: mcp-builder
  path: mcp/mcp-builder
  description: Build, test, inspect, install, and deploy MCP servers with FastMCP in Python. Use when creating a new MCP server,
    wrapping an API or database as MCP tools, exposing resources or prompts, or preparing a FastMCP server for Claude Code,
    Cursor, or HTTP deployment.
- name: mcp-builder
  path: mcp/mcp-builder
  description: Guide for creating high-quality MCP (Model Context Protocol) servers that enable LLMs to interact with external
    services through well-designed tools. Use when building MCP servers to integrate external APIs or services, whether in
    Python (FastMCP) or Node/TypeScript (MCP SDK).
- name: native-mcp
  path: mcp/native-mcp
  description: Use the native-mcp CLI to list, configure, auth, and call MCP servers/tools directly (HTTP or stdio), including
    ad-hoc servers, config edits, and CLI/type generation.
- name: native-mcp
  path: mcp/native-mcp
  description: 'MCP client: connect servers, register tools (stdio/HTTP).'
- name: omniroute-gateway
  path: mcp/omniroute-gateway
  description: Use when operating OmniRoute/Corporate Agent Route through its MCP gateway for account/provider connection management,
    model/catalog/health checks, routing smoke tests, quotas, and tool-scope diagnostics. This is not the generic OpenAI-compatible
    routing design skill.
