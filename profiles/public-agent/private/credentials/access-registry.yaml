# Access Registry — TEMPLATE
# Map agent lanes to their service accounts
# Replace with real service-to-credential mappings

agents:
  default:
    services: []
  corporate:
    services: []
  public:
    services: []
