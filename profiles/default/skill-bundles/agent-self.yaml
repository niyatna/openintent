name: agent-self
skills:
- draith-doctrine-check
- how-to-interview
- default-capability-router
- default-presence-memory
- saryn-archive
description: 'Category bundle: all 11 skills under `agent-self` for Agent.'
instruction: Use this only when the user intentionally wants the whole category/folder
  context. For normal work, prefer the lean core bundle plus exact routed skills to
  avoid context noise.
